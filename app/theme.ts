"use client";

import { createTheme } from "@mui/material/styles";

const theme = createTheme({
  palette: {
    primary: {
      main: "#E33E33",
    },
    secondary: {
      main: "#DFDAD6",
    },
  },
});

export default theme;
